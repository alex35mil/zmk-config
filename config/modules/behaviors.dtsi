&lt {
    tapping-term-ms = <TAPPING_TERM>;
};

&sl {
    release-after-ms = <5000>;
};

&caps_word {
    /delete-property/ ignore-modifiers;
};

ZMK_HOLD_TAP(tc_fast,
   tapping-term-ms = <TAPPING_TERM_FAST>;
   quick-tap-ms = <QUICK_TAP>;
   bindings = <&mo>, <&kp>;
)
