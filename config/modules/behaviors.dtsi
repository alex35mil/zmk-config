&lt {
    tapping-term-ms = <165>;
};

&caps_word {
    /delete-property/ ignore-modifiers;
};

#define __     &trans
#define ____   &trans
#define _XX_   &none
#define XXXX   &none
#define _TODO_ &none

#define HYPER &kp LS(LC(LG(LALT)))
#define INPUT &kp LG(LC(LA(SPACE)))

ZMK_HOLD_TAP(lt_fast,
   tapping-term-ms = <130>;
   bindings = <&mo>, <&kp>;
)
