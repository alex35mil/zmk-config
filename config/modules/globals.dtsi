#define TAPPING_TERM 165
#define TAPPING_TERM_MEDIUM 150
#define TAPPING_TERM_FAST 130

#define QUICK_TAP  175
#define PRIOR_IDLE 90

#define __                &trans // key on one of 6th cols that don't exist on my keyboard
#define _____             &trans // transparent key
#define _XXX_             &none  // noop key
#define XXXXXXXXXXXXXXXXX &none  // key on a layer that activates this layer
#define _TODO_            &none  // TODO key

#define HYPER LS(LC(LG(LALT)))

#define MENU_FOCUS LC(F2)
#define TRAY_FOCUS F20
#define MISSION_CONTROL LG(LC(LA(M)))
#define STAGE_MANAGER LG(LC(LA(S)))
#define WIDGETS LG(LC(LA(N)))
